def finstr(desstr,substr):
    count = 0
    length = len(desstr)
    if substr not in desstr:
        print('请输入正确的字符串。')
    else:
        for each1 in range(length-1):
            while

dessrtr = input('原字符串:')
substr = input('需要查找的字符串：')

